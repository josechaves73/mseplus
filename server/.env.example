# Variables de Entorno - EJEMPLO
# Copia este archivo como .env.production y completa con tus valores reales

# Entorno
NODE_ENV=production
PORT=3001

# Base de Datos MySQL (obtén estos datos de cPanel → MySQL Databases)
DB_HOST=localhost
DB_USER=tu_usuario_cpanel
DB_PASSWORD=tu_password_mysql
DB_NAME=tu_base_datos

# JWT Secret (genera una clave segura - puedes usar: openssl rand -base64 32)
JWT_SECRET=genera_una_clave_segura_muy_larga_aqui

# Cloudinary (opcional - solo si usas subida de imágenes)
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret